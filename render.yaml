services:
  # 1. PostgreSQL Database
  - type: pserv
    name: educonnect-db
    env: image
    plan: free
    region: ohio

  # 2. Backend API Service
  - type: web
    name: educonnect-api
    env: node
    plan: free
    region: ohio
    buildCommand: pnpm install && pnpm --filter @workspace/api-server build
    startCommand: cd artifacts/api-server && pnpm start
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: educonnect-db
          property: connectionString
      - key: NODE_ENV
        value: production

  # 3. Frontend Static Site
  - type: web
    name: educonnect-frontend
    env: static
    plan: free
    region: ohio
    buildCommand: pnpm install && pnpm --filter @workspace/educonnect-haiti build
    staticPublishPath: ./artifacts/educonnect-haiti/dist/public
    envVars:
      - key: NODE_ENV
        value: production
      - key: VITE_API_URL
        fromService:
          type: web
          name: educonnect-api
          property: host
